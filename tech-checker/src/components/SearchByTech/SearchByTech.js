import React, { Component } from 'react';
import { connect } from 'react-redux';

class SearchByTech extends Component {
    render() {
        return (
            <div>
                <p>This is where you will search by Tech...this will be a bit more complicated beacuse of the area and distance factor.</p>
            </div>
        );
    }
}

export default SearchByTech;